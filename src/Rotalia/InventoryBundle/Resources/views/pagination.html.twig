{#
 @var pager PropelModelPager
 @var url string
#}
<div class="pagination-links">
    {% if pager.page > pager.firstPage %}
    <a href="{{ url | replace({'__page__':pager.previousPage}) }}">&laquo; Eelmised</a>
    {% endif %}

    {% for i in pager.getLinks(5) %}
    <a href="{{ url | replace({'__page__':i}) }}"{% if i == pager.page %} class="current"{% endif %}>{{ i }}</a>
    {% endfor %}

    {% if pager.page < pager.lastPage %}
    <a href="{{ url | replace({'__page__':pager.nextPage}) }}">Järgmised &raquo;</a>
    {% endif %}
</div>
